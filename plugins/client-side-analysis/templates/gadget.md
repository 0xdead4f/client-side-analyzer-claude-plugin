# Gadgets

<!-- Solvers: APPEND new gadgets below this line. Do not overwrite existing entries. -->

<!--
## Gadget: [Short descriptive title]
- **Source**: [type] `[param/name]` at `[file:line]`
- **What it does**: [One sentence — e.g., "postMessage handler forwards attacker data to fetch() without origin check"]
- **Usable as**: [proxy / arbitrary fetch / DOM write primitive / cookie setter / storage poisoner / redirect primitive / etc]
- **Chains with**: [What other primitive or condition would make this exploitable — e.g., "XSS sink on the fetched response page" or "CORS misconfiguration on target API"]
---
-->
